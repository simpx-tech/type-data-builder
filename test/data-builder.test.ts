describe("Data Builder", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it("should create a new entity, initialize it's fields and salve it's variation", () => {
    // DEV
  });
});
